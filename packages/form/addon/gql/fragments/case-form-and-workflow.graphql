fragment CaseFormAndWorkflow on Case {
  id
  meta
  workflow {
    id
    slug
  }
  document {
    id
    form {
      id
      slug
    }
  }
  family {
    id
    meta
    workflow {
      id
      slug
    }
    document {
      id
      form {
        id
        slug
      }
    }
  }
}
