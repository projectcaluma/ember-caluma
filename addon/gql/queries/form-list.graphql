# import * from 'ember-caluma/gql/fragments/form-info'

query FormList($isArchived: Boolean = false) {
  allForms(isArchived: $isArchived) {
    edges {
      node {
        ...FormInfo
      }
    }
  }
}
