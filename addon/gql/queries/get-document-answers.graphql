#import 'ember-caluma/gql/fragments/field-answer'

query($id: ID!) {
  allDocuments(id: $id) {
    edges {
      node {
        id
        answers {
          edges {
            node {
              ...FieldAnswer
            }
          }
        }
      }
    }
  }
}
