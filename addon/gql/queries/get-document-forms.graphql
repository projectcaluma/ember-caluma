#import 'ember-caluma/gql/fragments/field-question'

query($id: ID!) {
  allDocuments(id: $id) {
    edges {
      node {
        id
        form {
          id
          slug
          questions {
            edges {
              node {
                ...FieldQuestion
              }
            }
          }
        }
      }
    }
  }
}
