#import 'ember-caluma/gql/fragments/field-question'

query($slug: String!) {
  allForms(slug: $slug) {
    edges {
      node {
        slug
        name
        questions {
          edges {
            node {
              ...FieldQuestion
            }
          }
        }
      }
    }
  }
}
