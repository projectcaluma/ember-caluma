{{component labelComponent}}

{{#power-select-multiple
	selected=selected
	options=(or availableFormatValidators.lastSuccessful.value (array))
	onchange=(action "updateValidators")
	as |item|}}
	{{item.name}}
{{/power-select-multiple}}

{{component hintComponent}}
{{component errorComponent}}