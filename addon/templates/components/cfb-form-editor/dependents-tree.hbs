<div {{did-insert (perform this.data)}}>
  {{#unless data.isRunning}}
    <ul class="uk-nav">
      {{#each this.data.lastSuccessful.value as |form| }}
        <CfbFormEditor::DependentsTree::NavigationList @form={{form}} />
      {{/each}}
    </ul>
  {{/unless}}
</div>